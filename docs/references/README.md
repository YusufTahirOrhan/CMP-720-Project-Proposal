# Local Reference Files

This folder can hold local source PDFs used during research.

The original DeFT paper should be placed here as:

```text
DeFT_A_Deadlock-Free_and_Fault-Tolerant_Routing_Algorithm_for_2.5D_Chiplet_Networks.pdf
```

That PDF is intentionally ignored by `.gitignore` to avoid committing third-party paper content while keeping the local project workspace reproducible.
